import React from "react";
import ReactDOM from "react-dom/client";
import Produkty from "./components/MPutra";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <Produkty></Produkty>
  </React.StrictMode>
);
